# Copie para .env (não versionado) ou defina no painel do host (Render, etc.)

# Recomendado em produção: chave longa para JWT (ou JWT_SECRET / SECRET_KEY_FILE)
# SECRET_KEY=
# JWT_SECRET=
# SECRET_KEY_FILE=/caminho/arquivo-somente-leitura

# Opcional: master (padrão em auth_util.py se vazio)
# MASTER_USERNAME=
# MASTER_PASSWORD=

# DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname

# CORS (opcional): https://app.exemplo.com,https://outro.com
# CORS_ORIGINS=

# Backup por e-mail (opcional; master aciona na UI)
# BACKUP_SMTP_HOST=smtp.gmail.com
# BACKUP_SMTP_PORT=587
# BACKUP_SMTP_USER=
# BACKUP_SMTP_PASSWORD=
# BACKUP_EMAIL_TO=
